<Page x:Class="EstateManager.View.LoaderView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:fa="http://schemas.fontawesome.io/icons/"
      xmlns:local="clr-namespace:EstateManager.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoaderView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <fa:ImageAwesome  Grid.Row="0" Icon="CircleOutlineNotch" Spin="True" SpinDuration="2" Width="200" VerticalAlignment="Bottom"/>
        <TextBlock Grid.Row="1" Text="DataBase Loading" TextAlignment="Center" VerticalAlignment="Top" FontSize="30" FontWeight="Bold" Foreground="Blue"/>
    </Grid>
</Page>
